let name = prompt("Введите имя: ");
let age = prompt("Введите возраст: ")

if (age > 22)
    alert('Welcome, ' + name)
if (age < 18)
    alert('You are not allowed to visit this website');
if (age = 18, 19, 20, 21, 22)
    var button;
var question = confirm("Are you sure you want to continue?");
if (question == true) {
    button = alert("Welcome, " + name);
} else {
    button = alert("You are not allowed to visit this website");
};

